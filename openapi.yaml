# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: House API
    version: 0.0.1
paths:
    /house/chat/start:
        post:
            tags:
                - House
            description: 发起在线聊天
            operationId: House_StartChat
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/StartChatRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/StartChatReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /house/personal-recommend:
        get:
            tags:
                - House
            description: 个性化推荐列表，根据用户浏览习惯推荐
            operationId: House_PersonalRecommendList
            parameters:
                - name: userId
                  in: query
                  schema:
                    type: string
                - name: page
                  in: query
                  schema:
                    type: integer
                    format: int32
                - name: pageSize
                  in: query
                  schema:
                    type: integer
                    format: int32
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/HouseRecommendReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /house/recommend:
        get:
            tags:
                - House
            description: 普通推荐列表
            operationId: House_RecommendList
            parameters:
                - name: page
                  in: query
                  schema:
                    type: integer
                    format: int32
                - name: pageSize
                  in: query
                  schema:
                    type: integer
                    format: int32
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/HouseRecommendReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /house/reserve:
        post:
            tags:
                - House
            description: 预约看房
            operationId: House_ReserveHouse
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/ReserveHouseRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ReserveHouseReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        HouseRecommendData:
            type: object
            properties:
                total:
                    type: string
                list:
                    type: array
                    items:
                        $ref: '#/components/schemas/HouseRecommendItem'
            description: 推荐列表数据
        HouseRecommendItem:
            type: object
            properties:
                title:
                    type: string
                    description: house_id字段已完全移除
                description:
                    type: string
                price:
                    type: number
                    format: double
                area:
                    type: number
                    format: double
                layout:
                    type: string
                imageUrl:
                    type: string
        HouseRecommendReply:
            type: object
            properties:
                code:
                    type: integer
                    format: int32
                msg:
                    type: string
                data:
                    $ref: '#/components/schemas/HouseRecommendData'
            description: 推荐列表响应
        ReserveHouseData:
            type: object
            properties:
                success:
                    type: boolean
                reservationId:
                    type: string
            description: 预约数据
        ReserveHouseReply:
            type: object
            properties:
                code:
                    type: integer
                    format: int32
                msg:
                    type: string
                data:
                    $ref: '#/components/schemas/ReserveHouseData'
            description: 预约看房响应
        ReserveHouseRequest:
            type: object
            properties:
                landlordId:
                    type: string
                userId:
                    type: string
                userName:
                    type: string
                houseId:
                    type: string
                houseTitle:
                    type: string
                reserveTime:
                    type: string
            description: 预约看房请求
        StartChatData:
            type: object
            properties:
                chatId:
                    type: string
                success:
                    type: boolean
            description: 发起聊天数据
        StartChatReply:
            type: object
            properties:
                code:
                    type: integer
                    format: int32
                msg:
                    type: string
                data:
                    $ref: '#/components/schemas/StartChatData'
            description: 发起聊天响应
        StartChatRequest:
            type: object
            properties:
                reservationId:
                    type: string
                userId:
                    type: string
                landlordId:
                    type: string
                initialMessage:
                    type: string
            description: 发起聊天请求
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
tags:
    - name: House
